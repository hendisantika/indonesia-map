/mvnw text eol=lf
*.cmd text eol=crlf

# SQL migration files tracked by Git LFS
src/main/resources/db/migration/*.sql filter=lfs diff=lfs merge=lfs -text

# Force LF line endings for other SQL files (non-migration)
*.sql text eol=lf